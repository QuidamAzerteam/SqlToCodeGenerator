name: Tests

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.pull_request.head.repo.owner.login }}
      - run: echo ${{ github.event.pull_request.base.repo.owner.login }}
      - run: echo ${{ github.event.pull_request.repository.owner.login }}
      - run: echo ${{ github.event.pull_request.fork }}

      - name: 🚚 Get latest code
        uses: actions/checkout@v2.3.2
        with:
          fetch-depth: 0

      - name: 🐞 Setup PHP with Xdebug
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          coverage: xdebug

      - name: 🎼 Composer
        uses: php-actions/composer@v6

      - name: ✔ PHPUnit tests
        run: vendor/bin/phpunit --configuration=tests/phpunit.xml

      - name: 🔧 Fix PHPUnit reports paths
        working-directory: ./tests/generatedReports
        run: |
          sed -i 's@'$GITHUB_WORKSPACE'@/github/workspace@g' coverage.xml
          sed -i 's@'$GITHUB_WORKSPACE'@/github/workspace@g' junit.xml

      - name: 🔍 SonarCloud scan
        # Because only team can use secret
        if: ${{ github.event.pull_request.head.repo.owner.login == 'QuidamAzerteam'}}
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
